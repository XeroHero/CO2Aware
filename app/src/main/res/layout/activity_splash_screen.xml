<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0099cc"
    tools:context=".SplashScreen">

    <!-- The primary full-screen view. This can be replaced with whatever view
         is needed to present your content, e.g. VideoView, SurfaceView,
         TextureView, etc. -->
    <ImageView
        android:id="@+id/image_view_splash"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/carbon_footprint"
        android:layout_marginBottom="70dp">
    </ImageView>

    <!-- This FrameLayout insets its children based on system windows using
         android:fitsSystemWindows. -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true">

        <LinearLayout
            android:id="@+id/fullscreen_content_controls"
            style="?metaButtonBarStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:background="@color/black_overlay"
            android:orientation="horizontal"
            tools:ignore="UselessParent">

            <Button
                android:layout_width="wrap_content"
                android:id="@+id/journey_planner_btn"
                android:layout_below="@id/image_view_splash"
                android:text="APICaller BTN"
                android:layout_height="70dp"></Button>

            <Button
                android:layout_width="wrap_content"
                android:layout_height="70dp"
                android:id="@+id/green_tips_btn"
                android:layout_toRightOf="@id/journey_planner_btn"
                android:layout_below="@id/image_view_splash"
                android:text="Green Tips">

            </Button>


            <Button
                android:layout_width="wrap_content"
                android:layout_height="70dp"
                android:id="@+id/friends_btn"
                android:layout_below="@id/image_view_splash"
                android:layout_toRightOf="@id/green_tips_btn"
                android:text="Friends"></Button>


            <Button
                android:layout_width="wrap_content"
                android:layout_height="70dp"
                android:id="@+id/about"
                android:layout_below="@id/image_view_splash"
                android:layout_toRightOf="@id/green_tips_btn"
                android:text="About"></Button>
        </LinearLayout>
    </FrameLayout>

</FrameLayout>